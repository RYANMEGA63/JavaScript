// Math = built-in object that provides a
//        collection of propreties and methods

Math.PI
Math.E

let x;
let y;
let z;

x = -10;
y = 2;
z = 9;

// z = Math.round(x);   rounds the number
// z = Math.floor(x);   always rounds down
// z = Math.ceil(x);    always rounds up
// z = Math.trunc(x);   eliminate any decimal places
// z = Math.pow(x, y);  x raised to the power of y
// z = Math.sqrt(121);  square root
// z = Math.log(x);     log
// z = Math.cos(x);     
// z= Math.sin(x);      all with radians
// z = Math.tan(x);
// z = Math.abs(x);     absolute value
// z = Math.sign(x);    to indicate if its positive or negative returning +1 or -1
// let result = Math.max(x, y, z)
// let result = Math.min(x, y, z)


document.getElementById('result').textContent = result;